Run OBI sync for the current project.

Follow `~/.obi/protocol/OBI_PROTOCOL.md`. Detect the project, read repo context
and existing Obsidian docs, generate missing canonical docs, update active docs,
and write `Sync Report.md`.

